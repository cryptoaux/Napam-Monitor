{
  "name": "napams-monitor",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "monitor": "node monitor.js",
    "test": "c8 --all --include \"src/**/*.js\" --check-coverage --statements 80 --branches 75 --functions 80 --lines 80 node --test",
    "lint": "eslint .",
    "format:check": "prettier --check ."
  },
  "dependencies": {
    "pino": "^10.3.1",
    "playwright": "^1.55.0"
  },
  "devDependencies": {
    "c8": "^12.0.0",
    "eslint": "^10.9.1",
    "prettier": "^3.9.6"
  }
}
